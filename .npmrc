# pnpm: pass --config.blockExoticSubdeps=false when running pnpm install
# (viem subdependency "ox" is URL-resolved; pnpm 11 blocks this by default)
# Use: pnpm install --config.blockExoticSubdeps=false
# Or just use: npm install
